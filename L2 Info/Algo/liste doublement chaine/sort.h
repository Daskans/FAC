#ifndef SORT_H
#define SORT_H

#include "dlist.h"

/* Sort the element of the list in ascending order.
 * Returns the new head of the list or NULL on error. */
dlist_t *sort (dlist_t *list);

#endif /* SORT_H */